CREATE TABLE Bmi(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    bmi REAL NOT NULL,
    time TEXT NOT NULL
);

getListBmi:
SELECT  * FROM Bmi;

insertBmi:
INSERT INTO Bmi(
    bmi,
    time
) VALUES (?, ?);